# Package Notes

Ubuntu/Debian names live in `apt.txt`. Hyprland packaging varies by release; use official Ubuntu packages when present, a trusted PPA, or upstream build instructions for missing Hyprland ecosystem packages.

`optional.txt` lists integrations used by scripts but not required for the desktop to start.
